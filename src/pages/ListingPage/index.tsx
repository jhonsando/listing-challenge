import ListFilter from "containers/ListFilter";

const ListingPage = (): JSX.Element => {
  return (
    <section>
      <ListFilter />
      <div>List...</div>
    </section>
  );
};

export default ListingPage;
